# OpenAI_API_KEY is only needed when running with --ai (opt-in).
# Default mode is RSS-only and does not use OpenAI.
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
MAX_CONCURRENCY=5
OUTPUT_PATH=~/TechDigest/latest.html
